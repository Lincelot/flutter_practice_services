name: flutter_practice_services
description: service package
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

# Network
  dio: ^5.4.3
  retrofit: ^4.1.0

# Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0

# Utils
  dartz: ^0.10.1
  equatable: ^2.0.5
  json_annotation: ^4.9.0
  build_config: ^1.1.1
  get:

dev_dependencies:
  flutter_test:
    sdk: flutter

# Linter
  flutter_lints: ^2.0.0

# Database
  hive_generator: ^2.0.1

# Tool
  build_runner: ^2.4.9
  retrofit_generator: ^8.1.0
  json_serializable: ^6.7.1
  freezed: ^2.5.2

# Test
  mockito: ^5.4.0
  test: ^1.24.3

flutter:
